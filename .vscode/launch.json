{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Twisted",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/app/twisted/socket_app.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            "envFile": "${workspaceFolder}/app/.env",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/app"
            }
        }
    ]
}